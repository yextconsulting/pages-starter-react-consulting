const config = (plop) => {
  plop.load("./plopfiles/component.js");
  plop.load("@yext/sites-react-starters");

  // console.log(plop);
  // console.log(plop.getGeneratorList());
};

export default config;
